frontend:
    resource: "@FrontendBundle/Controller/"
    type:     annotation
    prefix:   /

categorias_galeria:
    resource: "@CategoriasGaleriaBundle/Controller/"
    type:     annotation
    prefix:   /

publicaciones:
    resource: "@PublicacionesBundle/Controller/"
    type:     annotation
    prefix:   /

backend:
    resource: "@BackendBundle/Controller/"
    type:     annotation
    prefix:   /backend

_imagine:
    resource: .
    type:     imagine
